name: Legacy Clean Plugin Test Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo 'This legacy clean-test release workflow is intentionally disabled for automatic releases.'
          echo 'Use .github/workflows/clean-release.yml as the canonical Clean Manager release pipeline.'
