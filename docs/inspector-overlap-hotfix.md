# Inspector overlap hotfix

The Pages inspector tab strip must remain in normal document flow and may never overlap the settings form below it. The hotfix forces a wrapping grid, explicit row height, normal button positioning, clear separation from the settings target and bounded controls inside the inspector.
