# V3 3.0.0-alpha.6 release notes

- Restores V1 Manager styling on canonical `vdm-*` admin pages, including **Opdateringer**.
- Keeps the V1 Updates page markup and CSS unchanged.
- Adds V3 `3.0.0-alpha.1` through `3.0.0-alpha.6` to the packaged release history.
- Extends the release-history parser to accept semantic prerelease labels.
- Retains the complete V1 history beginning with `0.1.93` below the V3 entries.
- Updates the visible updater notice to `Visual Designer Manager → Opdateringer`.
- Preserves Designer, viewport, frontend renderer, Alpha.5 site shell, layout model and storage migration.
